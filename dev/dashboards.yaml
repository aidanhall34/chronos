apiVersion: 1

providers:
  - name: "Chronos"
    type: file
    updateIntervalSeconds: 10
    options:
      path: /otel-lgtm/grafana/conf/provisioning/dashboards/chronos
      foldersFromFilesStructure: true
